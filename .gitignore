# Build output
target/
*.class

# Environment / secrets — Neon, Atlas, staging, prod connection strings never get committed.
# See docs/project-management/DATABASE_SETUP.md for how env vars are used instead.
.env
.env.*
*.env

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Claude Code local session settings
.claude/

# Local secrets — real SMTP/API credentials for local dev. NEVER committed; each dev
# keeps their own copy. See local-secrets.example.ps1 for the template.
local-secrets.ps1
*.local.ps1
